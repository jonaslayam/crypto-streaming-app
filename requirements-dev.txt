# Testing tools only. Runtime dependencies live in each service's own
# requirements.txt (producer/, processor/, optimization/, backtest/).
pytest==8.3.3
pytest-asyncio==0.24.0
fakeredis[lua]==2.24.1
ruff==0.7.4
